# 2026-06-20 — Initial Scaffold

**Commits**: `a1396c5` (scaffold with tree/history view), `247bea3` (initial
scaffold).

**What was built**: FastAPI backend implementing the full L1–L5 pipeline
(see [architecture/pipeline.md](../architecture/pipeline.md)) paired with a
Next.js 15 frontend carrying the core signal-display components from day
one — not bolted on later. The commit message for `247bea3` explicitly
frames this as "lays groundwork for tree/history view," meaning the
lineage/history UI (`SignalLineageTree.tsx`, `SignalHistoryPanel.tsx`) was
part of the intended shape from the start, not a later addition.

**Why this scope**: the detection layer
([concepts/signal-detectors.md](../concepts/signal-detectors.md)) and
scoring layer ([concepts/confluence-scoring.md](../concepts/confluence-scoring.md))
carry comments referencing prior art —
`gcp-app-w-mcp1/mcp-finance1/src/technical_analysis_mcp/signals.py` and
`gcp3/backend/schemas/signal_output.py` — indicating this app is a
consolidation/port of technical-analysis logic developed in an earlier
project (`gcp-app-w-mcp1`/`gcp3`), rebuilt as a standalone, more disciplined
package (frozen config constants, Pydantic schema contract, per-detector
timeout isolation) rather than a from-scratch design.
